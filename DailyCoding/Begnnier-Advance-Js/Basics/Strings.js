let myname = "vansh"
const name = new String('vrc')
let reop = 50

console.log(name)
console.log(myname + reop + " value")

console.log(`my name is ${myname} and repo is equal to ${reop}`)


// trim removes white space 
// to fixed fix  the decimal value 
// to precise it round of the value to nearest number based on value passed 
// to string converts the value to string

// Deep copy do not share same references 
// Shallow copy share same referencess